name: setup-python
description: 'common setup-python config'

inputs:
  python-version:
    description: "py version"
  python-version-file:
    description: "files like pyproject.toml"

runs:
  using: composite
  steps:
    - name: Using common Setup-Python ${{ inputs.python-version }}
      uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
      with:
        python-version: ${{ env.PY_VERSION }}
        python-version-file: ${{ inputs.python-version-file }}
        cache: pip
      env:
        PY_VERSION: ${{ inputs.python-version || (inputs.python-version-file == '' && '3.12') || '' }}
